# THIS IS THE ENV VAR FILE
#
# Tip: Next.js only exposes env vars to the client / browser bundle, if they're prefixed with "NEXT_PUBLIC_".
#      Otherwise, the env var is only available on the server-side.



# DATABASE - PostgreSQL Configuration

# PostgreSQL Database Configuration
POSTGRES_DB=markenmate
POSTGRES_USER=markenmate
POSTGRES_PASSWORD=markenmate_dev_password_CHANGE_IN_PRODUCTION
POSTGRES_PORT=5432
POSTGRES_CONTAINER=markenmate-postgres

# Database URL for Drizzle ORM
# For Docker: use service name 'postgres' as host
# For local development: use 'localhost' as host
DATABASE_URL=postgresql://markenmate:markenmate_dev_password_CHANGE_IN_PRODUCTION@localhost:5432/markenmate

# IMPORTANT: In Docker, change 'localhost' to 'postgres' in DATABASE_URL!
# DATABASE_URL=postgresql://markenmate:markenmate_dev_password_CHANGE_IN_PRODUCTION@postgres:5432/markenmate



# AUTHENTICATION

# A secret that is used for salting of generated hashes
# IMPORTANT: Change this in production! Use a strong random string (min. 32 characters).
BETTER_AUTH_SECRET=dev-secret-change-in-production-to-min-32-characters

# Base URL that the auth lib uses for operation. Should be the URL of deployment / localhost with right port
# We make this public because we also need client-side access to this one.
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000



# NODE ENVIRONMENT
NODE_ENV=development


